{
  "name": "capmind-cli-workspace",
  "private": true,
  "version": "0.3.2",
  "scripts": {
    "dev": "cargo run -- --help",
    "build": "cargo build",
    "lint": "cargo clippy --all-targets -- -D warnings",
    "test": "cargo test",
    "fmt": "cargo fmt --all",
    "fmt:check": "cargo fmt --all -- --check"
  }
}

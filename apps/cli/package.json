{
  "name": "capmind-cli-workspace",
  "private": true,
  "version": "0.2.9",
  "scripts": {
    "dev": "cargo run -- --help",
    "build": "cargo build",
    "lint": "cargo clippy --all-targets -- -D warnings",
    "test": "cargo test",
    "fmt": "cargo fmt --all -- --check"
  }
}
